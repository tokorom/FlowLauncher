# 仕様

- メイン画面に登録済みのアプリ一覧が縦に並ぶ
- 各アプリに対応したキーを押すとそのアプリが開く（すでに開いていればそのアプリに切り替える）
- 各行にはアプリアイコン、アプリ名、対応するキーを表示
- Listの一番下にアプリの追加ボタン
  - 追加画面
    - アプリを参照して選択
    - 対応するキー1文字を入力して設定
- Listのアプリを右クリックで削除できる

# デザイン

- できるだけOSのシステムカラーを使う
- できるだけOSの標準コントロールを使う
